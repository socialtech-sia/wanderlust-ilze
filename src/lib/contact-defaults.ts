/**
 * Запасные контакты на случай, когда site_settings ещё не доехали.
 *
 * Живут отдельным файлом, потому что нужны в нескольких серверных местах —
 * src/lib/contacts.server.ts (страницы) и src/lib/email/contacts.ts (письма),
 * куда React тянуть нельзя. Держать две копии одного адреса — верный способ
 * однажды поменять только одну.
 *
 * Запасной здесь ТОЛЬКО адрес почты. У телефона запасного значения нет
 * намеренно: показать не тот номер хуже, чем не показать никакого.
 */
export const DEFAULT_CONTACT_EMAIL = "info@wanderlust.lv";
